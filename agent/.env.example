# ═══════════════════════════════════════════════════════════════════════
# 🐔 CHICKEN BUFFETT — Environment Configuration
# ═══════════════════════════════════════════════════════════════════════

# ─── LLM Provider ─────────────────────────────────────────────────────
# At least one LLM provider is required

# OpenAI (recommended)
OPENAI_API_KEY=sk-...

# Anthropic (alternative)
# ANTHROPIC_API_KEY=sk-ant-...

# ─── Macro Oracle API ─────────────────────────────────────────────────
# The Macro Oracle API endpoint for market signals
MACRO_ORACLE_API_URL=https://macro-oracle-production.up.railway.app

# ─── Drift Protocol Trading (Solana) ──────────────────────────────────
# Your Solana wallet private key
DRIFT_PRIVATE_KEY=

# Use devnet for testing (true/false). ALWAYS start with devnet!
DRIFT_DEVNET=true

# Default leverage for auto-trades (1-20, recommended: 3)
DEFAULT_LEVERAGE=3

# Default position size in USD for auto-trades
DEFAULT_SIZE_USD=100

# ─── Auto-Trading ─────────────────────────────────────────────────────
# Enable automated signal execution (true/false)
# When enabled, the signal watcher will auto-execute trades on high-confidence signals
AUTO_TRADE_ENABLED=false

# Minimum confidence threshold for auto-trades (0-100, recommended: 80)
AUTO_TRADE_MIN_CONFIDENCE=80

# ─── Twitter/X ────────────────────────────────────────────────────────
# OAuth 1.0a credentials (App must have Read+Write permissions!)
# After changing permissions, REGENERATE the access token & secret!
TWITTER_AUTH_MODE=env
TWITTER_API_KEY=
TWITTER_API_SECRET_KEY=
TWITTER_ACCESS_TOKEN=
TWITTER_ACCESS_TOKEN_SECRET=

# Posting configuration
TWITTER_ENABLE_POST=true
TWITTER_POST_IMMEDIATELY=false
TWITTER_POST_INTERVAL_MIN=90
TWITTER_POST_INTERVAL_MAX=150
TWITTER_DRY_RUN=false
TWITTER_MAX_TWEET_LENGTH=280

# Engagement configuration
TWITTER_ENABLE_REPLIES=true
TWITTER_ENABLE_ACTIONS=true
TWITTER_ENABLE_DISCOVERY=true

# Target accounts to proactively engage with (comma-separated usernames)
TWITTER_TARGET_USERS=solaboratory,DriftProtocol,ColosseumOrg,DriftFDN,JupiterExchange,MarginFi,phantom,solana

# Engagement check interval (minutes)
TWITTER_ENGAGEMENT_INTERVAL_MIN=20
TWITTER_ENGAGEMENT_INTERVAL_MAX=45
TWITTER_MAX_ENGAGEMENTS_PER_RUN=3

# ─── Social Poster (Custom) ──────────────────────────────────────────
# Custom posting intervals (in milliseconds)
# Default: 90-180 min between posts
SOCIAL_POST_MIN_INTERVAL_MS=5400000
SOCIAL_POST_MAX_INTERVAL_MS=10800000

# ─── Database ─────────────────────────────────────────────────────────
# PostgreSQL URL (optional — uses PGLite in-memory if not set)
# DATABASE_URL=postgresql://user:pass@host:5432/chicken_buffett

# ─── Server ───────────────────────────────────────────────────────────
# Server port (default: 3000)
# SERVER_PORT=3000
